# sdlc-kit

本リポジトリは、AI エージェント（Claude Code 等）とチーム開発を進めるための型（ワークフロー・安全機構・文書規範）を、既存リポジトリへ段階的に導入できる単位として提供する配布キットである。**1人で固めた実践的な型を、チームが最小限の手間で回せる形にして配る**ことを目的とする。

## Overview

エージェントを走らせると、ボトルネックはコード生成から検証と意図伝達へ移る。「何を約束したか」「誰が承認したか」「何を確かめて完了としたか」はコードに残らない。1人なら頭の中で完結するが、複数人の開発では共有できなくなる。本キットは、その合意と検証の軌跡をリポジトリ内に残すための置き場と手順を提供する。

型として配るのは、エージェントが自力で判断・規律を維持できない領域に限る。各単位の根拠となる設計思想は **[docs/design-philosophy.md](docs/design-philosophy.md)** に置く。各単位の `docs/ops/` は、この設計思想から導かれた規範をリポジトリへ適用できる形にしたものである。

---

## Units

導入する機能は、目的に応じて「単位（Unit）」として切り出してある。

| 引数 | 単位名 | 役割 |
|---|---|---|
| `single` | `flow-single` | **単独作業フロー**（合意を PR 本文に残す軽量フロー。担当者が実質1人の場合向け） |
| `issue` | `flow-issue` | **Issue 作業フロー**（合意を `issues/` に残す三役分業フロー。チーム開発向け） |
| `mvp` | `phase-mvp` | **立ち上がり期の駆動文書**（`PLAN.md` で終了条件を、`JUDGE.md` で設計判断を記録） |
| `guarantee` | `phase-guarantee` | **リリース後の駆動文書**（`docs/guarantees.md` で振る舞いの保証台帳を運用） |
| `report` | `daily-report` | **日報生成**（push 済みコミットから、Issue や PR を追わない関係者向けに日報を作成） |
| `walkthrough` | `walkthrough` | **中核機能の解説書**（重要機能の入出力や検証手順を段階的に理解・確認できる資料） |

### Required Units

以下の単位は規律を保つ基盤として、どの構成を選んでも常に自動で導入される。

- **`doc-style`**: リポジトリ内の Markdown やコメントの文章規範と、コミットメッセージの言語設定（`.claude/skills/jp-writing/`）
- **`guards`**: `main` への直接 push、force push、`gh pr merge` を機械的に拒否する安全機構
- **`branch-pr`**: ブランチと PR の運用規約、およびマージ済みローカルブランチの自動整理フック

---

## Installation

本リポジトリを clone し、取り込み先リポジトリと単位を指定して実行する。

```bash
git clone https://github.com/yktsnet/sdlc-kit.git
cd sdlc-kit

# 例1: 1人開発向けの最小構成
./install.sh ~/repos/myrepo single

# 例2: チーム開発向け（Issue 運用 + MVP 文書 + 保証台帳）
./install.sh ~/repos/myrepo issue mvp guarantee

# 例3: 既存リポに保証台帳（guarantee）のみを追加
./install.sh ~/repos/myrepo guarantee
```

引数は「取り込み先・作業フロー・追加単位」の順序で指定する。作業フローは省略できる。

### Installation Rules

- **作業フローは1つだけ選ぶ**: `issue` と `single` は同居させない。選び方は [docs/task-flows.md](docs/task-flows.md) を参照。
- **作業フローを選ぶと `daily-report` も同梱される**: 作業フローは PR をこまめに出す運用を前提とし、増えた PR の一覧性を日報が担保する（`report` 単体での導入も可能）。
- **上書き前の確認**: 取り込み先に同名ファイルがあり中身が異なる場合は差分を表示して確認する。無断で上書きはしない。
- **リポジトリ単位で自己完結**: 各単位はリポジトリ内の `docs/ops/` や設定ファイルとして配置する（`~/.claude/` などのグローバル領域には展開しない）。
- **取り込み後はそのリポジトリの管理物になる**: コピー後は取り込み先のリポジトリ内で修正し、配布元へは戻さない。配布元の更新差分は `./install.sh --diff ~/repos/myrepo` で確認し、手動で取り込みを判断する。

### Setup

インストール完了後、対象リポジトリ内で以下の作業を行う。

1. **設定スニペットの統合**: `.claude/settings.*.snippet.json` を `.claude/settings.json` へマージし、スニペットファイルを削除する。
2. **プレースホルダーの入力**: `<検証コマンド>` などの未設定項目をリポジトリ固有のコマンドに置き換える。
   ```bash
   grep -rn "<[^>]*>" .claude/skills docs/ops
   ```
3. **`CLAUDE.md` への参照追記**: 導入したフェーズや作業フローへの参照を記載する。単位の規約本文を書き写さず、参照のみを置く。
4. **個別手順の確認**: コピーされた各単位の `docs/ops/` 配下の文書を確認する。

---

## Design Philosophy

### Two Driving Documents

開発文書には寿命がある。単一の仕様書を永続させようとせず、フェーズごとに駆動文書を交代させる（詳細は [docs/lifecycle.md](docs/lifecycle.md)）。

- **MVP 期**: `PLAN.md`（何ができたら終わりか）と `JUDGE.md`（なぜその実装なのか）。README へ昇華した段階で削除する足場であり、永続を求めない。
- **Issue ドリブン期**: 保証台帳（`docs/guarantees.md`）。「何を約束し、何を約束していないか」だけを記し、各約束は対応するテストが継続検証する。約束が破られればテストが失敗するため、仕様が陳腐化しない。

台帳が正式運用に上がる瞬間が交代点となる。

### Guarantee-Driven Development (GDD)

振る舞いの保証（何が成り立つべきか）の宣言は人間（user）が承認し、テストの実装はエージェントが書く。

TDD がテストを先に書く規律だとすれば、**GDD は約束の承認を先に行う規律**である。人間の役割はテストコードを書くことから、約束を承認することへ移る。テストコードそのものが正なのではなく、承認された保証を実行可能な形に書き下ろしたものが正となる。

### Role Separation

決定と実行を同じ時間の中で進めると、決定が実装の都合に引きずられて検証できなくなる。

- **`flow-issue`（三役の分業）**:
  - **user（人間）**: 保証の承認、レビュー、マージ
  - **相談者（エージェント）**: 調査と Issue 設計（実装は行わない）
  - **実行者（エージェント）**: Issue に基づく実装とローカルコミット（リモートには触れない）
- **`flow-single`（2者の協調）**: 実装者と user の2者で進め、合意事項は PR 本文に残す。

どちらのフローでも、リモートに反映されるのは user がローカルでレビューしたものに限定する。硬直を避けるため、Issue 化を省略できる経路（障害対応、単発の明示例外、ロジックに触れない差分）も定めている。

### Consistency and Comparability

同じ形式の文書が担当者の数だけ並ぶと、完了条件の切り方も、捨てた選択肢の書き方も、検証の証拠も、書式の差ではなく中身の差として読めるようになる。成果物が標準化されることで作業の現在地を把握しやすくなり、この型を回して開発すること自体が育成の役割を果たす。

日報（`daily-report`）は他の文書と目的が異なる。Issue や PR が「リポジトリと変更がどうなっているか」を記録するのに対し、日報は「その人が1日に何を動かしたか」に答え、外部の関係者に向けて書く。そのため日報のみ担当者ごとに分かれる。

### Mechanical Enforcement and Guardrails

禁止事項は `CLAUDE.md` ではなく、`settings.json` の deny 設定やフックに置く。文書で注意を促すよりも、機構で遮断するほうが確実に守られる。

フックの利点は、拒否と同時に正しい経路へ誘導できる点にある。エージェントは拒否されると別の手を試すため、次に何をすべきかを拒否メッセージで指定する。さらに、セッション開始時にマージ済みのローカルブランチを掃除し、main に居残っていないかを報告する仕組み（`branch-pr`）も備える。

---

## Repository Structure

| ディレクトリ / ファイル | 役割 |
|---|---|
| `units/` | 取り込んで使う配布物（[units/README.md](units/README.md)） |
| `docs/` | 配布物の根拠（[design-philosophy.md](docs/design-philosophy.md)）、駆動文書の交代（[lifecycle.md](docs/lifecycle.md)）、作業フローの選び方（[task-flows.md](docs/task-flows.md)） |
| `install.sh` | 指定した単位を取り込み先へコピーするスクリプト |

---

## Background

個人のインフラ設定リポジトリ [yktsnet/dotfiles-public](https://github.com/yktsnet/dotfiles-public) で運用してきた仕組みから、特定環境に依存しない汎用的な部分を切り出した。

同じ手順を他の開発者が再現できるようにすることを目的とする。配布元の設定は Nix や特定のホスト環境、自作シェル関数を前提としているが、本キットではそれらの依存を外し、型として機能する部分のみを残した。

配布元で使っている一括マージ用のシェル関数などは配らず、worktree を切ってエージェントを起動し、レビューを経て push・PR・マージへ進むという**手順**を [flow-issue の workflow.md](units/flow-issue/docs/ops/workflow.md) に記載している。実行は標準的な `git` と `gh` CLI で十分に行える。

---

## License

MIT
